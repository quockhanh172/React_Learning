//100 created 101  process, 102 finish
enum STATUS{
    created=100, process=101, finish=102
};

let todoStatus : STATUS = STATUS.created;
console.log(todoStatus);

let student:Student = {
    id:1,
    name:"khanh",
    class:"2A"
}

console.log(student);

let studentList: StudentList=[1,2];
console.log(studentList);
